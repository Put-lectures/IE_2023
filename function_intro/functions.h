#ifndef FUNCTIONS_H
#define FUNCTIONS_H

#include <cmath>
#include <iostream>
char menu();
double calculate_pi(int k = 10000);
void pi();
void stats(float *tab, int n, float &min, float &max, float &mean);
int get_array(float *tab_);
void calculate_stats(float *tab, int n);

#endif // FUNCTIONS_H
